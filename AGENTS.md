# AGENTS.md — Repository Guidance for artlab

## Tech Stack
- Static HTML (`index.html`) and custom domain configuration (`CNAME`).

## Rules & Constraints
- Always create and work on a new feature branch (e.g., `feature/ui-update`).
- Do not modify `CNAME` unless explicitly instructed.
- Keep changes clean, semantically valid, and self-contained in HTML/CSS.
- Show a summary of file changes after editing.
